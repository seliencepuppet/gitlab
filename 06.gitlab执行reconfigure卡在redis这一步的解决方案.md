
```shell
[root@localhost ~]# systemctl restart gitlab-runsvdir
[root@localhost ~]# 
            - execute the ruby block wait for redis service socket

            - execute the ruby block wait for redis service socket
          - execute the ruby block reload_log_service
      Recipe: gitlab::gitlab-rails
        * execute[clear the gitlab-rails cache] action run          - execute the ruby block reload_log_service
      Recipe: gitlab::gitlab-rails
        * execute[clear the gitlab-rails cache] action run
          - execute /opt/gitlab/bin/gitlab-rake cache:clear
      
      Running handlers:
There was an error running gitlab-ctl reconfigure:

exit

      Running handlers complete
      Chef Client failed. 111 resources updated in 05 minutes 59 seconds

          - execute /opt/gitlab/bin/gitlab-rake cache:clear
      
      Running handlers:
There was an error running gitlab-ctl reconfigure:

exit

      Running handlers complete
      Chef Client failed. 111 resources updated in 02 minutes 16 seconds

[root@localhost ~]# 
[root@localhost ~]# 
[root@localhost ~]# 
[root@localhost ~]# /opt/gitlab/bin/gitlab-rake cache:clear
[root@localhost ~]# 
[root@localhost ~]# 
[root@localhost ~]# 
[root@localhost ~]# 
[root@localhost ~]# systemctl restart gitlab-runsvdir
[root@localhost ~]# 
[root@localhost ~]# systemctl restart gitlab-runsvdir
[root@localhost ~]# 
```
